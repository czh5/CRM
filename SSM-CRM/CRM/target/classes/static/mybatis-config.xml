<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- settings控制mybatis的全局行为 -->
    <settings>
        <!-- 设置mybatis输出日志 -->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>

    <!-- 设置别名 -->
    <typeAliases>
        <!-- 实体类所在包名 -->
        <package name="com.heng.crm.settings.domain"/>
        <package name="com.heng.crm.workbench.domain"/>
    </typeAliases>

    <!-- 映射文件位置 -->
    <mappers>
        <!-- <mapper resource=""/> -->
        <!-- 读取包下的所有映射文件 -->
        <package name="com.heng.crm.settings.dao"/>
        <package name="com.heng.crm.workbench.dao"/>
    </mappers>
</configuration>